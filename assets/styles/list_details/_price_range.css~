.price-range {
  display: flex;
  flex-direction: column;
  gap: 1rem;
  border: 1px solid var(--border-color);
  border-radius: 0.5rem;
  padding: 1rem 0;
  .no-verified {
    font-family: "Exo-semi-bold", sans-serif;
    font-size: 1rem;
    color: var(--main-color);
    text-align: center;
    padding: 1rem;
    background-color: rgba(255, 6, 4, 0.1);
  }
  .prices-range {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1rem;
    font-family: "Exo-semi-bold", sans-serif;
    color: var(--main-color);

  }

  .request-sent {
    font-family: "Exo-semi-bold", sans-serif;
    font-size: 1rem;
    color: #FDBA00;
    text-align: center;
    padding: 1rem;
    background-color: rgba(253, 186, 0, 0.1);
    display: flex;
    align-items: center;
    gap: 0.5rem;
    justify-content: center;
  }
  .user {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-inline: 1rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid #b2bbc6;
    .user-avatar {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      overflow: hidden;
      img {
        width: 100%;
        height: 100%;
        object-fit: cover;
      }
    }
    .user-info {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
      font-family: "Jost-regular", sans-serif;
      color: #0b1427;
    }
  }
  .contact {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-inline: 1rem;
    .wrapper {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-family: "Jost-regular", sans-serif;
      color: var(--main-color);
    }
  }

  .opening-hours {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    .opening-hours-title {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 0.5rem;
      margin-inline: 1rem;
      h3 {
        font-family: "Exo-semi-bold", sans-serif;
        font-size: 1rem;
        color: #47586E;
      }
      span {
        font-family: "Jost-regular", sans-serif;
        color: #15B097;
      }
    }
      .day-title, .time {
        font-family: "Jost-regular", sans-serif;
      }
      .day-title {
        color: #47586E;
      }
      .time {
        color: var(--primary-color);
      }
    .p-accordionheader,
    .p-accordioncontent[data-p-active="true"] {
      background-color: #F0F3F6;
    }
  }
  .action {
    display: flex;
    gap: 1rem;
    align-items: center;
    margin-inline: 1rem;

    .heart {
      display: flex;
      align-items: center;
      justify-content: center;
      min-width: 60px;
      height: 60px;
      border-radius: 50%;
      border: 1px solid var(--main-color);
      cursor: pointer;
      transition: all 0.3s ease-in-out;
      &:hover {
        background-color: rgba(255, 6, 4, 0.2);
      }
    }
  }
}
