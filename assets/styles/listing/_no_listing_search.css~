.listing-search-content {
  .no-listing-search {
    margin-top: 3rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    width: 500px;
    margin-inline: auto;
    text-align: center;
    @media screen and (max-width: 768px) {
      width: 100%;
    }
    .no-listing-img {
      width: 250px;
    }
    .no-listing-title {
      font-family: "Exo-semi-bold", serif;
      font-size: 1.2rem;
      color: var(--primary-color);
      @media screen and (max-width: 768px) {
        font-size: 0.8rem;
      }
    }
    .no-listing-description {
      font-family: "Exo-regular", serif;
      font-size: 1rem;
      color: var(--text-color);
      @media screen and (max-width: 768px) {
        font-size: 0.8rem;
      }
    }
  }
}
