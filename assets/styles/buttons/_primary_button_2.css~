.primary-button-2 {
  border-radius: 6px;
  background-color: var(--main-color);
  color: #ffffff;
  &:hover {
    background-color: #ffffff;
    color: var(--main-color);
    svg {
      path {
        fill: var(--main-color);
      }
    }
  }
}
