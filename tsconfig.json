{
  // https://nuxt.com/docs/guide/concepts/typescript
  "extends": "./.nuxt/tsconfig.json",
  "compilerOptions": {
    "strict": true, // Enables strict type-checking
    "noImplicitOverride": true // Enforces the `override` modifier for overrides
  }
}
